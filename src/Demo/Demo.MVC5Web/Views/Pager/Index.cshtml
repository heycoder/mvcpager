@using HeyCoder.Web.Mvc.Pager
<!DOCTYPE html>
<html>
<head>
    <title>HeyCoder.MvcPager Demo</title>
    <link href="~/Content/css/pager.css" rel="stylesheet" />
</head>
<body>
    @Html.Pager("default", new { p = Guid.NewGuid().ToString() }, new PagerOption(100, int.Parse(Request["pageindex"] ?? "1"), 10))
    @Html.Pager("default", new { p = Guid.NewGuid().ToString() }, new PagerOption(100, int.Parse(Request["pageindex1"] ?? "1"), 10) { PageIndexParamName = "pageindex1" })
</body>
</html>
